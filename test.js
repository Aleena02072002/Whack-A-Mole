function mathRan() {
    const Res =  Math.random();

    document.getElementById("result").textContent = Res; //textContent with assignment: assign the Res to id tag "result", which is the p tag
}

